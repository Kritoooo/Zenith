# JSON Formatter

将 JSON 格式化为可读的树状或原始文本，并支持压缩输出。

## 功能
- 格式化：2/4 空格缩进
- 压缩：输出为单行
- Tree / Raw 视图切换
- 展开 / 折叠所有节点
- 一键复制输出内容

## 使用步骤
1. 在 Input 粘贴 JSON。
2. 点击 Format 或 Minify。
3. 在 Output 里切换 Tree 或 Raw。
4. 需要复制时点击 Copy。

## 注意
- 仅支持标准 JSON（不支持注释或尾逗号）。
- 若浏览器阻止剪贴板，请切换到 Raw 模式手动复制。

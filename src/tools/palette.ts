import type { ToolCategory } from "@/tools/types";

export const CATEGORY_ACCENTS: Record<ToolCategory, string> = {
  dev: "var(--accent-blue)",
  design: "var(--accent-orange)",
  media: "var(--accent-pink)",
  utility: "var(--accent-green)",
};
